version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - misspell
    - gosec
    - bodyclose
    - noctx

formatters:
  enable:
    - gofmt
    - goimports

linters-settings:
  errcheck:
    check-blank: true
  gosec:
    severity: medium
    confidence: medium
  misspell:
    locale: US

issues:
  exclude-rules:
    # Exclude generated files
    - path: types\.gen\.go
      linters:
        - gosec
        - errcheck
    - path: _test\.go
      linters:
        - gosec
        - errcheck
