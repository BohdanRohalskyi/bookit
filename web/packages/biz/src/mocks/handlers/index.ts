import type { RequestHandler } from 'msw'

// Auth handlers are added in Phase 6.
// Import and spread them here as they are created:
// import { authHandlers } from './auth'

export const handlers: RequestHandler[] = []
